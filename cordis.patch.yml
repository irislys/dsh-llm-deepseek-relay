# dsh-llm-deepseek-relay bundle patch.
# The bundle registers a DeepSeekAdapter route per configured relay supplier.
# Configuration lives in $DSH_HOME/deepseek-relay.config.yaml (see README).
- insert:
    - id: llm-deepseek-relay
      name: 'dsh-llm-deepseek-relay'
